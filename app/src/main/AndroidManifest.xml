<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mntic.trueque">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trueque">
        <activity
            android:name=".LoginActivity"
            android:exported="true"> <!--Se cambio a true- sug de android-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="@string/title_reg" />
        <activity
            android:name=".AddCourseActivity"
            android:exported="false"
            android:label="@string/title_new" />
        <activity
            android:name=".EditCourseActivity"
            android:exported="false"
            android:label="@string/title_edit" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>